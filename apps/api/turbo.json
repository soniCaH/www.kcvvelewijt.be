{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "vitest*.config.ts", "wrangler.workerd-test.jsonc"],
      "outputs": []
    }
  }
}
