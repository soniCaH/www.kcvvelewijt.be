// First paint: 3 featured + 21 grid. Load-more then appends BATCH_SIZE per
// click (NEWS-1, #2237) — replaces the old infinite scroll.
export const INITIAL_TOTAL = 24;
export const BATCH_SIZE = 12;
