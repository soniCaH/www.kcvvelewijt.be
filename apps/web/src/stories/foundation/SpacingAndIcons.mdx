import {
  MagnifyingGlass, X, List, CaretDown, ArrowRight, ArrowSquareOut, CheckCircle,
  Info, Warning,
  House, Envelope, Phone, MapPin,
  User, UsersThree,
  Heart, Handshake, SoccerBall, Ticket,
  FacebookLogo, InstagramLogo, ShareNetwork, DownloadSimple,
} from '@/lib/icons.redesign';


export const SpacingRow = ({ token, value, px }) => (
  <div style={{ display: 'flex', alignItems: 'center', gap: '16px', padding: '6px 0', borderBottom: '1px solid #edeff4' }}>
    <div style={{ width: '120px', flexShrink: 0, fontSize: '12px', fontFamily: 'monospace', color: '#9a9da2' }}>{token}</div>
    <div style={{ width: '60px', flexShrink: 0, fontSize: '12px', color: '#9a9da2' }}>{value}</div>
    <div style={{ width: '50px', flexShrink: 0, fontSize: '12px', color: '#9a9da2' }}>{px}</div>
    <div style={{ height: '16px', background: 'var(--color-jersey)', borderRadius: '2px', width: px, maxWidth: '200px' }} />
  </div>
);

export const IconGrid = ({ icons, label }) => (
  <div style={{ marginBottom: '24px' }}>
    <div style={{ fontSize: '13px', fontWeight: 600, color: '#9a9da2', marginBottom: '8px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>{label}</div>
    <div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px' }}>
      {icons.map(({ Icon, name }) => (
        <div key={name} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px', padding: '10px', background: '#f8f9fb', borderRadius: '8px', minWidth: '72px' }}>
          <Icon size={20} color="#31404b" />
          <div style={{ fontSize: '10px', color: '#9a9da2', fontFamily: 'monospace', textAlign: 'center' }}>{name}</div>
        </div>
      ))}
    </div>
  </div>
);

# Spacing & Icons

---

## Custom Spacing Tokens

These extend Tailwind's default spacing scale. All values are defined in `src/app/globals.css`.

<div>
  <div style={{ display: 'flex', gap: '16px', padding: '4px 0', fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace', borderBottom: '2px solid #cacaca', marginBottom: '4px' }}>
    <div style={{ width: '120px' }}>Token</div>
    <div style={{ width: '60px' }}>Value</div>
    <div style={{ width: '50px' }}>px</div>
    <div>Visual</div>
  </div>
  <SpacingRow token="spacing-0_75" value="0.75rem" px="12px" />
  <SpacingRow token="spacing-2_5" value="2.5rem" px="40px" />
  <SpacingRow token="spacing-15" value="3.75rem" px="60px" />
  <SpacingRow token="spacing-17_5" value="4.375rem" px="70px" />
  <SpacingRow token="spacing-22_5" value="5.625rem" px="90px" />
</div>

> `spacing-35` (35rem) is the hero image height — too wide to show inline.

---

## Breakpoints

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Notes</th></tr></thead>
  <tbody>
    <tr><td><code>sm</code></td><td>640px</td><td>Tailwind default</td></tr>
    <tr><td><code>md</code></td><td>768px</td><td>Foundation medium</td></tr>
    <tr><td><code>desk</code></td><td>960px</td><td>Gatsby parity breakpoint</td></tr>
    <tr><td><code>lg</code></td><td>1024px</td><td>Tailwind default</td></tr>
    <tr><td><code>xl</code></td><td>1280px</td><td>Foundation xlarge</td></tr>
    <tr><td><code>2xl</code></td><td>1536px</td><td>Tailwind default</td></tr>
  </tbody>
</table>

---

## Max Widths

<table>
  <thead><tr><th>Token</th><th>Value</th><th>px</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>max-w-inner</code></td><td>60rem</td><td>960px</td><td>Article content, narrow layouts</td></tr>
    <tr><td><code>max-w-inner-lg</code></td><td>70rem</td><td>1120px</td><td>Wide content areas</td></tr>
    <tr><td><code>max-w-outer</code></td><td>90rem</td><td>1440px</td><td>Full-bleed container cap</td></tr>
  </tbody>
</table>

---

## Shadows

<table>
  <thead><tr><th>Token</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>shadow-sm</code></td><td>Cards, subtle elevation</td></tr>
    <tr><td><code>shadow</code></td><td>Default card shadow</td></tr>
    <tr><td><code>shadow-md</code></td><td>Raised elements</td></tr>
    <tr><td><code>shadow-lg</code></td><td>Modals, overlays</td></tr>
    <tr><td><code>shadow-card-hover</code></td><td>Card hover state (<code>0 12px 32px rgba(0,0,0,0.15)</code>)</td></tr>
    <tr><td><code>shadow-green</code></td><td>Focus glow (<code>0 0 6px 1px rgba(74,207,82,0.5)</code>)</td></tr>
    <tr><td><code>shadow-input</code></td><td>Form input inset</td></tr>
    <tr><td><code>shadow-input-focus</code></td><td>Form input focus state</td></tr>
  </tbody>
</table>

---

## Icon Catalogue

Icons are [Phosphor](https://phosphoricons.com) glyphs, rendered fill-weight
through the wrapper module `@/lib/icons.redesign`. Each wrapper defaults
`weight="fill"` and accepts `size` (number or string), `className`, `color`,
`aria-label`, and `aria-hidden`. Import the glyph you need by name from that
module — there is no string-name lookup or `<Icon>` wrapper component.

The grids below show a representative subset; see `src/lib/icons.redesign.ts`
for the full export list.

<IconGrid label="Navigation & UI" icons={[
  { Icon: MagnifyingGlass, name: 'MagnifyingGlass' },
  { Icon: X, name: 'X' },
  { Icon: List, name: 'List' },
  { Icon: CaretDown, name: 'CaretDown' },
  { Icon: ArrowRight, name: 'ArrowRight' },
  { Icon: ArrowSquareOut, name: 'ArrowSquareOut' },
]} />

<IconGrid label="Status & Feedback" icons={[
  { Icon: Info, name: 'Info' },
  { Icon: CheckCircle, name: 'CheckCircle' },
  { Icon: Warning, name: 'Warning' },
]} />

<IconGrid label="Social & Sharing" icons={[
  { Icon: FacebookLogo, name: 'FacebookLogo' },
  { Icon: InstagramLogo, name: 'InstagramLogo' },
  { Icon: ShareNetwork, name: 'ShareNetwork' },
  { Icon: DownloadSimple, name: 'DownloadSimple' },
]} />

<IconGrid label="Contact & Location" icons={[
  { Icon: House, name: 'House' },
  { Icon: Envelope, name: 'Envelope' },
  { Icon: Phone, name: 'Phone' },
  { Icon: MapPin, name: 'MapPin' },
]} />

<IconGrid label="People & Club" icons={[
  { Icon: User, name: 'User' },
  { Icon: UsersThree, name: 'UsersThree' },
  { Icon: Heart, name: 'Heart' },
  { Icon: Handshake, name: 'Handshake' },
  { Icon: SoccerBall, name: 'SoccerBall' },
  { Icon: Ticket, name: 'Ticket' },
]} />

---

## Usage in code

```tsx
// Named import from the Phosphor wrapper module @/lib/icons.redesign
import { SoccerBall, MapPin } from "@/lib/icons.redesign";

<SoccerBall size={20} className="text-jersey-deep" />
<MapPin size={16} aria-label="Locatie" />
```

---

## Redesign / Phase 0 — Layout, Rotation, Shadow, Motion

The redesign introduces four new groups of layout primitives: container widths
for prose vs. default content, a four-step rotation pool for taped-card looks,
hard-edged paper shadows for the cream surface, and motion durations for
typical interactions. All values are defined in `src/app/globals.css`.

### Container widths

`--container-prose` (680px) caps long-form article text at a comfortable
measure; `--container-wide` (1040px) widens editorial media (article images,
video) beyond prose while staying clearly bounded; `--container-default`
(1200px) is the standard page-content width between the cream surface
gutters. `--max-width-outer` (1440px) — already present from earlier work
— remains the full-bleed cap.

<div style={{ marginBottom: '24px' }}>
  <div style={{ height: '8px', background: 'var(--color-jersey)', width: 'var(--container-prose)', maxWidth: '100%', marginBottom: '4px' }} />
  <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginBottom: '12px' }}>--container-prose · 680px (article body cap)</div>
  <div style={{ height: '8px', background: 'var(--color-jersey)', width: 'var(--container-wide)', maxWidth: '100%', marginBottom: '4px' }} />
  <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginBottom: '12px' }}>--container-wide · 1040px (wide editorial media)</div>
  <div style={{ height: '8px', background: 'var(--color-jersey-deep)', width: 'var(--container-default)', maxWidth: '100%', marginBottom: '4px' }} />
  <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>--container-default · 1200px (standard page content)</div>
</div>

### Rotation pool

Four small angles compose the taped-card aesthetic. Pick one per card to keep
the layout from feeling regimented; never apply uniform rotation across a
grid. The pool is intentionally narrow so the page never tips into chaos.

<div style={{ display: 'flex', gap: '24px', flexWrap: 'wrap', alignItems: 'center', marginBottom: '24px', padding: '32px 16px', background: 'var(--color-cream)', borderRadius: '8px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '80px', height: '80px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-paper-edge)', boxShadow: 'var(--shadow-paper-sm)', transform: 'rotate(var(--rotate-tape-a))', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink)' }}>-2.5°</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '8px' }}>--rotate-tape-a</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '80px', height: '80px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-paper-edge)', boxShadow: 'var(--shadow-paper-sm)', transform: 'rotate(var(--rotate-tape-b))', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink)' }}>-1.5°</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '8px' }}>--rotate-tape-b</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '80px', height: '80px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-paper-edge)', boxShadow: 'var(--shadow-paper-sm)', transform: 'rotate(var(--rotate-tape-c))', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink)' }}>1°</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '8px' }}>--rotate-tape-c</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '80px', height: '80px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-paper-edge)', boxShadow: 'var(--shadow-paper-sm)', transform: 'rotate(var(--rotate-tape-d))', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink)' }}>2°</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '8px' }}>--rotate-tape-d</div>
  </div>
</div>

### Paper shadows

The redesign uses hard-edged offset shadows ("paper shadows") for cards on the
cream surface — they read like risograph print rather than soft web glows. The
soft shadow stays available for chrome that should sit subtly above content
(dropdowns, sticky bars).

<div style={{ display: 'flex', gap: '32px', flexWrap: 'wrap', alignItems: 'flex-start', marginBottom: '24px', padding: '32px 24px', background: 'var(--color-cream)', borderRadius: '8px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-ink)', boxShadow: 'var(--shadow-paper-sm)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-paper-sm</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>4px offset</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-ink)', boxShadow: 'var(--shadow-paper-md)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-paper-md</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>6px offset</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-ink)', boxShadow: 'var(--shadow-paper-lift)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-paper-lift</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>8px offset (hover lift)</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-paper-edge)', boxShadow: 'var(--shadow-soft)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-soft</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>diffuse, for chrome</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-ink)', boxShadow: 'var(--shadow-photo-tape)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-photo-tape</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>2px / 4px asymmetric (R9)</div>
  </div>
  <div style={{ textAlign: 'center' }}>
    <div style={{ width: '120px', height: '120px', background: 'var(--color-cream-soft)', border: '1px solid var(--color-ink)', boxShadow: 'var(--shadow-photo-tape-lift)' }} />
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2', marginTop: '12px' }}>--shadow-photo-tape-lift</div>
    <div style={{ fontSize: '11px', fontFamily: 'monospace', color: '#9a9da2' }}>4px / 8px asymmetric hover (R9)</div>
  </div>
</div>

The two `--shadow-photo-tape*` tokens use an asymmetric offset (2 / 4 and 4 / 8) different from the symmetric paper shadows. Photos lean forward; cards rest flat. Both vocabularies coexist — see `<TapedFigure>` for the photo idiom.

### Motion

Three motion tokens cover the redesign's interaction palette. Interactive
motion previews live in `<TapedCard>` Storybook later in Phase 1 — this page
documents the tokens only.

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>Value</th>
      <th>Use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>--motion-fast</code></td>
      <td><code>150ms ease-out</code></td>
      <td>Hover state changes — colour, opacity, underline</td>
    </tr>
    <tr>
      <td><code>--motion-base</code></td>
      <td><code>240ms cubic-bezier(0.2, 0.8, 0.2, 1)</code></td>
      <td>Default UI motion — drawer/menu open, focus ring</td>
    </tr>
    <tr>
      <td><code>--motion-tape</code></td>
      <td><code>300ms ease-out</code></td>
      <td>TapedCard rotation snap + lift animation</td>
    </tr>
  </tbody>
</table>
