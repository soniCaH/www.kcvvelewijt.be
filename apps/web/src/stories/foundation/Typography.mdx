import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundation/Typography" />

export const metaBase = { fontSize: '11px', color: '#cacaca', fontFamily: 'monospace' };

export const TypeSample = ({ token, size, px, weight = 400, sample = 'KCVV Elewijt Football Club' }) => (
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px', padding: '8px 0', borderBottom: '1px solid #edeff4' }}>
    <div style={{ width: '96px', flexShrink: 0, fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace' }}>{token}</div>
    <div style={{ width: '64px', flexShrink: 0, fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace' }}>{size}</div>
    <div style={{ width: '48px', flexShrink: 0, fontSize: '11px', color: '#9a9da2' }}>{px}</div>
    <div style={{ fontSize: `var(${token})`, fontFamily: 'var(--font-family-body)', fontWeight: weight, color: '#31404b', lineHeight: 1.2, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{sample}</div>
  </div>
);

export const FontSample = ({ label, cssVar, sample, size = '1.25rem', weight = 400, note }) => (
  <div style={{ padding: '16px 20px', background: '#f8f9fb', borderRadius: '8px', marginBottom: '12px', borderLeft: '3px solid #4acf52' }}>
    <div style={{ display: 'flex', gap: '8px', alignItems: 'baseline', marginBottom: '10px' }}>
      <span style={{ fontSize: '11px', fontWeight: 600, color: '#9a9da2', fontFamily: 'monospace', textTransform: 'uppercase', letterSpacing: '0.05em' }}>{label}</span>
      <span style={{ ...metaBase }}>{cssVar}</span>
      {note && <span style={{ ...metaBase }}>— {note}</span>}
    </div>
    <div style={{ fontSize: size, fontFamily: `var(${cssVar})`, fontWeight: weight, color: '#31404b', lineHeight: 1.3 }}>{sample}</div>
  </div>
);

# Typography

All type tokens are defined in `src/app/globals.css`. The body font is **Montserrat** (loaded via
Adobe Typekit); the heading font is **Quasimoda**. System-safe fallbacks are specified for all families.

---

## Font Families

Fonts are loaded via Adobe Typekit. The samples below render live from the CSS variables.

<FontSample label="font-title" cssVar="--font-family-title" size="2rem" weight={700} sample="KCVV Elewijt Football Club" note="All headings h1–h6" />
<FontSample label="font-body" cssVar="--font-family-body" size="1rem" weight={400} sample="De wedstrijd begint om 15:00. Alle supporters zijn welkom op het veld van KCVV Elewijt." note="Default body text" />
<FontSample label="font-alt" cssVar="--font-family-alt" size="2.5rem" weight={400} sample="KCVV" note="Accent / display (stenciletta)" />
<FontSample label="font-mono" cssVar="--font-family-mono" size="1.25rem" weight={700} sample="2 — 1   90′   45+2′" note="Scores, timestamps, countdowns, keyboard shortcuts" />

---

## Type Scale

<div style={{ marginBottom: '16px' }}>
  <div style={{ display: 'flex', gap: '16px', padding: '4px 0', fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace', borderBottom: '2px solid #cacaca' }}>
    <div style={{ width: '96px' }}>Token</div>
    <div style={{ width: '64px' }}>rem</div>
    <div style={{ width: '48px' }}>px</div>
    <div>Sample</div>
  </div>
  <TypeSample token="--font-size-xs" size="0.75rem" px="12px" sample="Body small / meta / labels" />
  <TypeSample token="--font-size-sm" size="0.8125rem" px="13px" sample="Captions, helper text" />
  <TypeSample token="--font-size-base" size="1rem" px="16px" sample="Default body text" />
  <TypeSample token="--font-size-lg" size="1.125rem" px="18px" sample="Lead paragraph / intro text" />
  <TypeSample token="--font-size-xl" size="1.25rem" px="20px" sample="Card titles / section labels" />
  <TypeSample token="--font-size-2xl" size="1.375rem" px="22px" sample="Sub-headings (mobile h2)" />
  <TypeSample token="--font-size-3xl" size="1.5rem" px="24px" sample="Section headings (desktop h3)" />
  <TypeSample token="--font-size-4xl" size="1.75rem" px="28px" sample="Page title (mobile h1)" />
  <TypeSample token="--font-size-5xl" size="2rem" px="32px" sample="Feature headings (desktop h2)" />
  <TypeSample token="--font-size-stat" size="2.5rem" px="40px" sample="Stats / large numbers" />
  <TypeSample token="--font-size-6xl" size="3rem" px="48px" sample="Hero title (desktop h1)" />
  <TypeSample token="--font-size-hero" size="clamp(3rem, 7vw, 5.5rem)" px="48–88px" sample="Hero heading (responsive)" />
</div>

---

## Heading Scale

Headings are set globally in CSS — no per-component overrides needed.

<table>
  <thead><tr><th>Heading</th><th>Mobile</th><th>Desktop (≥768px)</th></tr></thead>
  <tbody>
    <tr><td>h1</td><td>28px (<code>4xl</code>)</td><td>48px (<code>6xl</code>)</td></tr>
    <tr><td>h2</td><td>22px (<code>2xl</code>)</td><td>32px (<code>5xl</code>)</td></tr>
    <tr><td>h3</td><td>20px (<code>xl</code>)</td><td>24px (<code>3xl</code>)</td></tr>
    <tr><td>h4</td><td>18px (<code>lg</code>)</td><td>22px (<code>2xl</code>)</td></tr>
    <tr><td>h5</td><td>16px (<code>base</code>)</td><td>16px (<code>base</code>)</td></tr>
    <tr><td>h6</td><td>13px (<code>sm</code>)</td><td>13px (<code>sm</code>)</td></tr>
  </tbody>
</table>

---

## Font Weights

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>font-normal</code></td><td>400</td><td>Body text</td></tr>
    <tr><td><code>font-medium</code></td><td>500</td><td>Emphasis, labels</td></tr>
    <tr><td><code>font-bold</code></td><td>700</td><td>Headings, CTAs</td></tr>
  </tbody>
</table>

---

## Line Heights

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>leading-hero</code></td><td>0.9</td><td>Hero headings (tight stacking for large type)</td></tr>
    <tr><td><code>leading-tight</code></td><td>1.2</td><td>Headings</td></tr>
    <tr><td><code>leading-snug</code></td><td>1.25</td><td>Subheadings, blockquotes</td></tr>
    <tr><td><code>leading-normal</code></td><td>1.5</td><td>Short body</td></tr>
    <tr><td><code>leading-relaxed</code></td><td>1.6</td><td>Article content</td></tr>
    <tr><td><code>leading-loose</code></td><td>1.75</td><td>Default body (<code>&lt;body&gt;</code>)</td></tr>
  </tbody>
</table>

---

## Letter Spacing

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>tracking-label</code></td><td>0.14em</td><td>Uppercase kicker / label text</td></tr>
    <tr><td><code>tracking-caps</code></td><td>0.08em</td><td>Uppercase links / footer captions</td></tr>
  </tbody>
</table>

---

## Special utilities

```tsx
<h2 className="font-title font-bold leading-tight">Heading text</h2>
<p className="font-body leading-loose">Body text</p>
<span className="font-alt text-4xl">KCVV</span>        {/* stenciletta accent */}
<span className="font-mono font-bold text-3xl">2 — 1</span>  {/* scores, times, countdowns */}
```
