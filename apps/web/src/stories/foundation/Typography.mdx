

import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundation/Typography" />

export const metaBase = { fontSize: '11px', color: '#cacaca', fontFamily: 'monospace' };

export const TypeSample = ({ token, size, px, weight = 400, font = 'var(--font-family-body)', sample = 'KCVV Elewijt Football Club' }) => (
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px', padding: '8px 0', borderBottom: '1px solid #edeff4' }}>
    <div style={{ width: '96px', flexShrink: 0, fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace' }}>{token}</div>
    <div style={{ width: '64px', flexShrink: 0, fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace' }}>{size}</div>
    <div style={{ width: '48px', flexShrink: 0, fontSize: '11px', color: '#9a9da2' }}>{px}</div>
    <div style={{ fontSize: `var(${token})`, fontFamily: font, fontWeight: weight, color: '#31404b', lineHeight: 1.2, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{sample}</div>
  </div>
);

export const FontSample = ({ label, cssVar, sample, size = '1.25rem', weight = 400, note }) => (
  <div style={{ padding: '16px 20px', background: '#f8f9fb', borderRadius: '8px', marginBottom: '12px', borderLeft: '3px solid #4acf52' }}>
    <div style={{ display: 'flex', gap: '8px', alignItems: 'baseline', marginBottom: '10px' }}>
      <span style={{ fontSize: '11px', fontWeight: 600, color: '#9a9da2', fontFamily: 'monospace', textTransform: 'uppercase', letterSpacing: '0.05em' }}>{label}</span>
      <span style={{ ...metaBase }}>{cssVar}</span>
      {note && <span style={{ ...metaBase }}>— {note}</span>}
    </div>
    <div style={{ fontSize: size, fontFamily: `var(${cssVar})`, fontWeight: weight, color: '#31404b', lineHeight: 1.3 }}>{sample}</div>
  </div>
);

# Typography

All type tokens are defined in `src/app/globals.css`.

<table>
  <thead><tr><th>Font</th><th>Source</th><th>Weights</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><strong>Freight Display Pro</strong></td><td>Adobe Typekit (deferred)</td><td>700, 900</td><td>Editorial headlines, hero display lines (<code>--font-display</code>)</td></tr>
    <tr><td><strong>Freight Big Pro</strong></td><td>Adobe Typekit (deferred)</td><td>900</td><td>Oversized magazine display (<code>--font-display-big</code>)</td></tr>
    <tr><td><strong>Freight Sans Pro</strong></td><td>Adobe Typekit (deferred)</td><td>400, 500, 600, 700</td><td>Body text, UI labels, buttons (<code>--font-family-body</code>)</td></tr>
    <tr><td><strong>IBM Plex Mono</strong></td><td><code>next/font/google</code> (self-hosted)</td><td>400, 600, 700</td><td>Scores, timestamps, keyboard shortcuts</td></tr>
  </tbody>
</table>

All three Freight families load via Adobe Typekit (`afterInteractive`, not render-blocking); IBM Plex Mono is
preloaded and self-hosted via `next/font/google` with `display: 'swap'`. Because the Freight families load
async, each is backed by a metric-matched (`size-adjust`) fallback — `Freight Sans Fallback` (body) and
`Freight Display Fallback` (display) in `globals.css` — so the swap doesn't shift layout.

> The legacy Quasimoda (headings) and Stenciletta (accent/display) Typekit
> families were retired in #2154. Headings use Freight Display via
> `--font-display`; **body moved from Montserrat to Freight Sans Pro in #2174**
> (`--font-family-body`), reversing master-design decision-log entry 19.
> Freight Sans defaults to oldstyle figures in prose; stat surfaces opt into
> tabular lining figures (`tabular-nums lining-nums`).

---

## Font Families

The samples below render live from the CSS variables.

<FontSample label="font-display" cssVar="--font-display" size="2.5rem" weight={900} sample="Laatste nieuws." note="Editorial headlines, hero display lines (Freight Display Pro)" />
<FontSample label="font-display-big" cssVar="--font-display-big" size="2.5rem" weight={900} sample="KCVV" note="Oversized magazine display (Freight Big Pro)" />
<FontSample label="font-body" cssVar="--font-family-body" size="1rem" weight={400} sample="De wedstrijd begint om 15:00. Alle supporters zijn welkom op het veld van KCVV Elewijt." note="Default body text (Freight Sans Pro)" />
<FontSample label="font-mono" cssVar="--font-family-mono" size="1.25rem" weight={700} sample="2 — 1   90′   45+2′" note="Scores, timestamps, countdowns, keyboard shortcuts (IBM Plex Mono)" />

> **Display vs Big — when to use which.** `--font-display` (Freight Display) is the default heading serif for all heading *text* — editorial headlines, section titles, hero lines, the italic accent word, and global `h1–h6` (weights 600–700). `--font-display-big` (Freight Big) is the heavier poster cut, **always `font-black` (900)**, reserved for the single largest heading tier (`<EditorialHeading size="display-2xl">`) and every big **number** — `<NumberDisplay>`, match scores, standings points, jersey-number discs, and monogram badges (numbers add `tabular-nums`).

---

## Type Scale

<div style={{ marginBottom: '16px' }}>
  <div style={{ display: 'flex', gap: '16px', padding: '4px 0', fontSize: '11px', color: '#9a9da2', fontFamily: 'monospace', borderBottom: '2px solid #cacaca' }}>
    <div style={{ width: '96px' }}>Token</div>
    <div style={{ width: '64px' }}>rem</div>
    <div style={{ width: '48px' }}>px</div>
    <div>Sample</div>
  </div>
  <TypeSample token="--font-size-xs" size="0.75rem" px="12px" sample="Body small / meta / labels" />
  <TypeSample token="--font-size-sm" size="0.8125rem" px="13px" sample="Captions, helper text" />
  <TypeSample token="--font-size-base" size="1rem" px="16px" sample="Default body text" />
  <TypeSample token="--font-size-lg" size="1.125rem" px="18px" sample="Lead paragraph / intro text" />
  <TypeSample token="--font-size-xl" size="1.25rem" px="20px" sample="Card titles / section labels" />
  <TypeSample token="--font-size-2xl" size="1.375rem" px="22px" weight={700} font="var(--font-display)" sample="Sub-headings (mobile h2)" />
  <TypeSample token="--font-size-3xl" size="1.5rem" px="24px" weight={700} font="var(--font-display)" sample="Section headings (desktop h3)" />
  <TypeSample token="--font-size-4xl" size="1.75rem" px="28px" weight={700} font="var(--font-display)" sample="Page title (mobile h1)" />
  <TypeSample token="--font-size-5xl" size="2rem" px="32px" weight={700} font="var(--font-display)" sample="Feature headings (desktop h2)" />
  <TypeSample token="--font-size-stat" size="2.5rem" px="40px" weight={900} font="var(--font-display-big)" sample="Stats / large numbers" />
  <TypeSample token="--font-size-6xl" size="3rem" px="48px" weight={700} font="var(--font-display)" sample="Hero title (desktop h1)" />
  <TypeSample token="--font-size-hero" size="clamp(3rem, 7vw, 5.5rem)" px="48–88px" weight={700} font="var(--font-display)" sample="Hero heading (responsive)" />
</div>

---

## Heading Scale

Headings are set globally in CSS — no per-component overrides needed.

<table>
  <thead><tr><th>Heading</th><th>Mobile</th><th>Desktop (≥768px)</th></tr></thead>
  <tbody>
    <tr><td>h1</td><td>28px (<code>4xl</code>)</td><td>48px (<code>6xl</code>)</td></tr>
    <tr><td>h2</td><td>22px (<code>2xl</code>)</td><td>32px (<code>5xl</code>)</td></tr>
    <tr><td>h3</td><td>20px (<code>xl</code>)</td><td>24px (<code>3xl</code>)</td></tr>
    <tr><td>h4</td><td>18px (<code>lg</code>)</td><td>22px (<code>2xl</code>)</td></tr>
    <tr><td>h5</td><td>16px (<code>base</code>)</td><td>16px (<code>base</code>)</td></tr>
    <tr><td>h6</td><td>13px (<code>sm</code>)</td><td>13px (<code>sm</code>)</td></tr>
  </tbody>
</table>

---

## Font Weights

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>font-normal</code></td><td>400</td><td>Body text</td></tr>
    <tr><td><code>font-medium</code></td><td>500</td><td>UI labels</td></tr>
    <tr><td><code>font-semibold</code></td><td>600</td><td>Buttons, badges</td></tr>
    <tr><td><code>font-bold</code></td><td>700</td><td>Headings, CTAs, scores</td></tr>
  </tbody>
</table>

---

## Line Heights

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>leading-hero</code></td><td>0.9</td><td>Hero headings (tight stacking for large type)</td></tr>
    <tr><td><code>leading-tight</code></td><td>1.2</td><td>Headings</td></tr>
    <tr><td><code>leading-snug</code></td><td>1.25</td><td>Subheadings, blockquotes</td></tr>
    <tr><td><code>leading-normal</code></td><td>1.5</td><td>Short body</td></tr>
    <tr><td><code>leading-relaxed</code></td><td>1.6</td><td>Article content</td></tr>
    <tr><td><code>leading-loose</code></td><td>1.75</td><td>Default body (<code>&lt;body&gt;</code>)</td></tr>
  </tbody>
</table>

---

## Letter Spacing

<table>
  <thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead>
  <tbody>
    <tr><td><code>tracking-label</code></td><td>0.14em</td><td>Uppercase kicker / label text</td></tr>
    <tr><td><code>tracking-caps</code></td><td>0.08em</td><td>Uppercase links / footer captions</td></tr>
  </tbody>
</table>

---

## Special utilities

```tsx
<h2 className="font-display font-black leading-tight">Heading text</h2>
<p className="font-body leading-loose">Body text</p>
<span className="font-display-big text-4xl">KCVV</span>      {/* oversized magazine display */}
<span className="font-mono font-bold text-3xl">2 — 1</span>  {/* scores, times, countdowns */}
```

---

## Redesign / Phase 0 — Editorial Type Scale

The redesign introduces a fluid editorial type scale built on Freight Display
Pro for headlines, Freight Sans Pro for running text, and IBM Plex Mono for
labels and timing data. Display sizes use `clamp()` so they breathe between
mobile and desktop without manual breakpoints.

The legacy Quasimoda / Stenciletta Typekit families were retired in #2154 —
headings now set in Freight Display via `--font-display`. The
`--font-display` / `--font-display-big` / `--font-family-body` / `--font-family-mono`
tokens are the canonical vocabulary for every primitive.

### Visual samples

The four samples below mix Freight Display, Freight Sans, and IBM Plex Mono so
the contrast between editorial display, running text, and label voice is
visible at a glance. Dutch diacritics (`é`, `ï`) and italic emphasis are
exercised on the display rows.

<div style={{ background: 'var(--color-cream)', padding: '2rem', borderRadius: '8px', marginBottom: '24px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ fontFamily: 'var(--font-display)', fontSize: 'var(--text-display-xl)', lineHeight: 'var(--text-display-xl--lh)', fontWeight: 900, color: 'var(--color-ink)', marginBottom: '0.5rem' }}>Laatste <em style={{ fontStyle: 'italic', fontWeight: 700 }}>nieuws.</em></div>
  <div style={{ fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink-muted)' }}>--font-display · --text-display-xl · weight 900 + italic 700</div>
</div>

<div style={{ background: 'var(--color-cream)', padding: '2rem', borderRadius: '8px', marginBottom: '24px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ fontFamily: 'var(--font-display)', fontSize: 'var(--text-display-lg)', lineHeight: 'var(--text-display-lg--lh)', fontWeight: 700, color: 'var(--color-ink)', marginBottom: '0.5rem' }}>Het rooster.</div>
  <div style={{ fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink-muted)' }}>--font-display · --text-display-lg · weight 700</div>
</div>

<div style={{ background: 'var(--color-cream)', padding: '2rem', borderRadius: '8px', marginBottom: '24px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ fontFamily: 'var(--font-body)', fontSize: 'var(--text-body-md)', lineHeight: 'var(--text-body-md--lh)', color: 'var(--color-ink)', marginBottom: '0.5rem' }}>Drie afdelingen, 320 kinderen, één idee: plezier eerst.</div>
  <div style={{ fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink-muted)' }}>--font-body · --text-body-md</div>
</div>

<div style={{ background: 'var(--color-cream)', padding: '2rem', borderRadius: '8px', marginBottom: '24px', border: '1px solid var(--color-paper-edge)' }}>
  <div style={{ fontFamily: 'var(--font-mono)', fontSize: 'var(--text-label)', letterSpacing: 'var(--text-label--tracking)', textTransform: 'uppercase', lineHeight: 1, color: 'var(--color-ink)', marginBottom: '0.5rem' }}>INTERVIEW · SNELTREIN · 8 MIN</div>
  <div style={{ fontFamily: 'monospace', fontSize: '11px', color: 'var(--color-ink-muted)' }}>--font-mono · --text-label · uppercase + 0.08em tracking</div>
</div>

### Token reference

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>Value</th>
      <th>Use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>--font-display</code></td>
      <td><code>"freight-display-pro", georgia, serif</code></td>
      <td>Editorial headlines, hero display lines</td>
    </tr>
    <tr>
      <td><code>--font-display-big</code></td>
      <td><code>"freight-big-pro", georgia, serif</code></td>
      <td>Oversized magazine-style display (occasional)</td>
    </tr>
    <tr>
      <td><code>--text-display-2xl</code></td>
      <td><code>clamp(3.5rem, 1.5rem + 8vw, 6rem)</code> · lh <code>1.0</code></td>
      <td>Page hero — largest editorial display</td>
    </tr>
    <tr>
      <td><code>--text-display-xl</code></td>
      <td><code>clamp(2.75rem, 1.5rem + 5vw, 4.5rem)</code> · lh <code>1.05</code></td>
      <td>Section hero, big section headlines</td>
    </tr>
    <tr>
      <td><code>--text-display-lg</code></td>
      <td><code>clamp(2rem, 1.25rem + 3vw, 3rem)</code> · lh <code>1.1</code></td>
      <td>Section heading (e.g. "Het rooster.")</td>
    </tr>
    <tr>
      <td><code>--text-display-md</code></td>
      <td><code>clamp(1.5rem, 1rem + 1.5vw, 2rem)</code> · lh <code>1.2</code></td>
      <td>Sub-section heading</td>
    </tr>
    <tr>
      <td><code>--text-display-sm</code></td>
      <td><code>clamp(1.25rem, 1rem + 1vw, 1.5rem)</code> · lh <code>1.3</code></td>
      <td>Card title, small editorial heading</td>
    </tr>
    <tr>
      <td><code>--text-body-lg</code></td>
      <td><code>1.125rem</code> · lh <code>1.55</code></td>
      <td>Lead paragraph, intro copy</td>
    </tr>
    <tr>
      <td><code>--text-body-md</code></td>
      <td><code>1rem</code> · lh <code>1.6</code></td>
      <td>Default running text</td>
    </tr>
    <tr>
      <td><code>--text-body-sm</code></td>
      <td><code>0.875rem</code> · lh <code>1.55</code></td>
      <td>Captions, secondary text</td>
    </tr>
    <tr>
      <td><code>--text-mono-md</code></td>
      <td><code>0.875rem</code> · lh <code>1.4</code></td>
      <td>Mono medium — match times, timestamps in body</td>
    </tr>
    <tr>
      <td><code>--text-mono-sm</code></td>
      <td><code>0.75rem</code> · lh <code>1.4</code></td>
      <td>Mono small — inline timing data, table cells</td>
    </tr>
    <tr>
      <td><code>--text-label</code></td>
      <td><code>0.6875rem</code> · lh <code>1</code></td>
      <td>Uppercase label / kicker — use with <code>--text-label--tracking</code></td>
    </tr>
    <tr>
      <td><code>--text-label--lh</code></td>
      <td><code>1</code></td>
      <td>Line-height companion for <code>--text-label</code></td>
    </tr>
    <tr>
      <td><code>--text-label--tracking</code></td>
      <td><code>0.08em</code></td>
      <td>Letter-spacing companion for <code>--text-label</code></td>
    </tr>
  </tbody>
</table>
